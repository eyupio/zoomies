# The zoomies.sh site.
#
# It publishes the docs that already live in docs/ rather than a second copy of
# them, so the website cannot drift from the repository. install.sh is copied to
# the site root by the deploy workflow, again from the one in the repository
# root, so `curl -fsSL https://zoomies.sh/install.sh | sh` and the file a
# contributor edits are the same file.

site_name: Zoomies
site_url: https://zoomies.sh/
site_description: >-
  Free, open-source self-hosted GitHub Actions runners with ephemeral jobs, a
  live web UI, autoscaling across your own hosts, and a migration path off
  static runners.
site_author: Zoomies contributors
copyright: Zoomies is AGPL-3.0 licensed.

repo_url: https://github.com/eyupio/zoomies
repo_name: eyupio/zoomies

docs_dir: docs
site_dir: site

theme:
  name: material
  # Sharing and structured metadata that Material does not emit by itself.
  # overrides/main.html extends the theme rather than replacing anything. It is
  # outside docs/ so the template is not also published as a page.
  custom_dir: overrides
  language: en
  favicon: brand/favicon.ico
  # The header renders at favicon scale, where v2.1 reserves the paw/swish.
  logo: brand/paw-swish-white.png
  icon:
    repo: fontawesome/brands/github
  # No `font:` key: the faces are self-hosted rather than fetched from Google
  # Fonts. overrides/main.html replaces the theme's font block, and
  # docs/stylesheets/ carries the @font-face rules and the woff2 files beside
  # it. A third-party font stylesheet is render-blocking, and when the host is
  # slow or unreachable the tab spins with nothing to show.
  features:
    - navigation.instant
    - navigation.instant.progress
    - navigation.tracking
    - navigation.sections
    - navigation.top
    - navigation.footer
    - toc.follow
    - search.suggest
    - search.highlight
    - search.share
    - content.code.copy
    - content.code.annotate
  palette:
    # The brand is a near-black identity, so dark is the default and light is
    # the alternative rather than the other way round.
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      primary: custom
      accent: custom
      toggle:
        icon: material/weather-sunny
        name: Switch to light
    - media: "(prefers-color-scheme: light)"
      scheme: default
      primary: custom
      accent: custom
      toggle:
        icon: material/weather-night
        name: Switch to dark

extra_css:
  - stylesheets/zoomies.css
  - stylesheets/animated-logo.css

extra_javascript:
  - javascripts/animated-logo.js

markdown_extensions:
  - abbr
  - admonition
  - attr_list
  - def_list
  - footnotes
  - md_in_html
  - tables
  - toc:
      permalink: true
      permalink_title: Link to this section
  - pymdownx.details
  # Material's icon sets, as `:material-webhook:` shortcodes. The home page's
  # feature cards and comparison table use them; they render as inline SVG that
  # inherits the text colour, so there is no icon image to theme or to go stale.
  - pymdownx.emoji:
      emoji_index: !!python/name:material.extensions.emoji.twemoji
      emoji_generator: !!python/name:material.extensions.emoji.to_svg
  - pymdownx.highlight:
      anchor_linenums: true
      line_spans: __span
      pygments_lang_class: true
  - pymdownx.inlinehilite
  - pymdownx.snippets
  # Diagrams are Mermaid, rendered in the browser and themed by Material to
  # follow whichever palette the reader is in. A diagram is a fenced block in
  # the same file as the prose it explains, so GitHub renders it too and there
  # is no exported image to go stale.
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format
  - pymdownx.tabbed:
      alternate_style: true
  - pymdownx.tasklist:
      custom_checkbox: true

plugins:
  # The built-in search index, told where a Zoomies identifier stops being one
  # word: the default separator does not split on a dot or a hyphen, so
  # `scheduler.scale_up_delay` and `zoomies-linux-x64` were each a single term
  # nobody would type in full.
  - search:
      separator: '[\s\-,:!=\[\]()"/]+|(?!\b)(?=[A-Z][a-z])|\.(?!\d)|&[lg]t;'

# Build-time SEO metadata: a real last-edited date per page for the sitemap and
# the sharing tags, and llms.txt generated from the navigation. It is a hook
# rather than a plugin so the docs build keeps to its pinned dependencies.
hooks:
  - hooks/seo.py
  # The header's latest release, stars and forks, resolved once per build and
  # rendered by overrides/partials/source.html. Material would otherwise fetch
  # them in the browser and cache them in the tab for as long as it lives.
  - hooks/source.py

extra:
  # Who builds Zoomies. overrides/partials/copyright.html prints this in the
  # footer of every page, beside the licence; the product's own footer says the
  # same thing from web/src/lib/links.ts, so a fork renames it in two places
  # rather than everywhere.
  developer:
    name: EyUp.io
    url: https://eyup.io
  social:
    - icon: fontawesome/brands/github
      link: https://github.com/eyupio/zoomies
      name: Zoomies on GitHub
  generator: false

# The navigation is ordered the way a fleet is run: the web UI first, because
# it is the primary way to configure and operate Zoomies; then the other ways
# in -- the command line, Docker Compose, a PaaS, the API -- which reach the
# same controller; then the reference. A page under "Running the fleet"
# describes a task from the UI and says where its CLI and API equivalents are.
nav:
  - Home: index.md
  - Quick start: quickstart.md
  - Running the fleet from the web UI:
      - The UI, page by page: ui.md
      - Hosts and pools: hosts-and-pools.md
      - Elastic CPU zoomies: elastic-cpu.md
      - Private hosts with Tailcat: private-hosts.md
      - Proxmox VE: proxmox.md
      - Migrating repositories: migration.md
      - Backup and restore: backup-and-restore.md
      - Upgrading: upgrading.md
      - Troubleshooting: troubleshooting.md
  - Other ways in:
      - Command line: cli.md
      - Docker Compose: compose.md
      - Deploying on a PaaS: paas.md
      - One-click deployment: marketplace.md
      - API: api-surface.md
      - Capacity-demand receiver: capacity-demand-receiver.md
  - Reference:
      - Configuration: configuration.md
      - Architecture: architecture.md
      - Naming and platforms: naming.md
      - Security: security.md
      - Runner startup stability: runner-startup-stability.md
      - Runtime compatibility: runtime-compatibility.md
      - Problem codes: problem-codes.md
      - Metrics: metrics.md
      - Provider contract: providers.md
      - Dependencies: dependencies.md
      - UI guidelines: ui-guidelines.md
      - Brand: brand.md
  - Runners in Docker: docker.md
  - Compared with ARC: actions-runner-controller.md
  - What it costs: costs.md
  - FAQ: faq.md

# The brand pack's own guide is published because docs/brand.md links to it, but
# it is not a section of the site's navigation.
not_in_nav: |
  brand/BRANDING_GUIDE.md

# A link in the docs that points nowhere is a broken promise, so a broken one
# fails the build rather than shipping.
strict: true
validation:
  links:
    absolute_links: ignore
    unrecognized_links: warn
    # A heading that gets reworded silently breaks every deep link into it, and
    # a link that lands at the top of a long page looks like it worked.
    anchors: warn
